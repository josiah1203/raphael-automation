openapi: 3.1.0
info:
  title: raphael-automation
  version: 0.1.0
  description: Triggers, actions, scheduling, cron jobs
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
